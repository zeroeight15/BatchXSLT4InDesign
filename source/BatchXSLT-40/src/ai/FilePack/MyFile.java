package ai.FilePack;public class MyFile {	private final String path;	private final String name;	private final int type;	//  this entry's type: 1 == file, 2 == dir	private final boolean locked_folder;	//  if type == 2 == dir: marke for folder containes a '##folderlocked' file		/*	* Set path and file name.	* Example: For the file "MyFile.class" in "classes\hansen\playground" we have:	*	 file = "MyFile.class" and	*	 path = "classes\hansen\playground"	*/	public MyFile(String path, String name) {		this.path = path;		this.name = name;		this.type = 1;		this.locked_folder = false;	}		public MyFile(String path, String name, int type) {		this.path = path;		this.name = name;		this.type = type;		if (type == 2) this.locked_folder = com.epaperarchives.batchxslt.BatchXSLTransform.has_lockfile(path);		else this.locked_folder = false;	}				/*	* check if this is a locked folder entry	*/	public boolean is_locked_folder() { return locked_folder; }				/*	* Get the Type of the file: 1 == file, 2 == dir	*/	public int getType() { return type; }			/*	* Get the name of the file	*/	public String getName() { return name; }		/*	* Get the path of the file	*/	public String getPath() { return path; }	}	